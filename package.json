{
  "name": "certifypro-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "vite --port 3000",
    "build": "vite build --emptyOutDir",
    "build:clean": "rm -rf dist && vite build",
    "deploy": "npm run build && vercel --prod --force",
    "preview": "vite preview"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "papaparse": "^5.5.3",
    "react": "^18.2.0",
    "react-dom": "^18.3.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.1.4"
  }
}